namespace std {
    typedef int/*implementation-defined*/ streamoff;
    typedef int/*implementation-defined*/ streamsize;

	template <class stateT> class fpos;
	
	template <class stateT> class fpos {
	 public:
		stateT state() const;
		void state(stateT);
	 private;
		stateT st; // exposition only
	};
}