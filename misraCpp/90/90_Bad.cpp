// 1은 정수 리터럴 입니다.(비준수)
if ( 1 && ( c < d ) )
//  c + d는 정수 연산의 결과입니다.(비준수)
if ( ( a < b ) && ( c + d ) ) 
//  u8_a 와 c+d 는 bool 타입이 아닙니다.(비준수)
if ( u8_a && ( c + d ) ) 
// 0은 정수 형식입니다.(비준수)
if ( !0 ) 
// ptr은 포인터 형식입니다.(비준수) 
if ( !ptr )